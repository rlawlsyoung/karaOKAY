import styled from 'styled-components';

const Music = () => {
  return <MusicContainer>ds</MusicContainer>;
};

const MusicContainer = styled.div`
  display: flex;
  background-color: transparent;
`;

export default Music;
